{
  "name": "laufbuddy-buddy-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && find lib -type f -name \"*.test.js\" ! -name \"*.emulator.test.js\" -exec node --test {} +",
    "test:buddy-transactions": "npm run build && CI=true XDG_CONFIG_HOME=/tmp/laufbuddy-firebase-cli-config ../node_modules/.bin/firebase emulators:exec --project demo-laufbuddy-buddy-transactions --only firestore \"node --test lib/buddyTransactionService.emulator.test.js\""
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "~5.9.2"
  }
}
